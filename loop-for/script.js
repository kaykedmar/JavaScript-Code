// Criamos um conjunto de frutas array
let fruits = ['Banana', 'orange', 'mango', 'morango'];

// Ordenamos as frutas em ordem alfabética (.sort()), reversa (.reverse())
fruits = fruits.sort().reverse();

// Mostramos cada fruta na ordem que está
for (let fruit of fruits) {
  console.log(fruit);
}




