// Inicialização: Definindo o valor inicial de i como 1
let i = 1;

// Enquanto i for menor que 10 + 1 (ou seja, menor que 11):
while (i < 10 + 1) {
  // Imprime o valor atual de i
  console.log(i);
  
  // Incrementa o valor de i por 1 para a próxima iteração
  i++;
}